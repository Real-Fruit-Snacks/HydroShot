<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 720 220" fill="none">
  <defs>
    <!-- Primary gradient: mauve to pink -->
    <linearGradient id="g1" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" stop-color="#cba6f7"/>
      <stop offset="40%" stop-color="#dbb9f9"/>
      <stop offset="100%" stop-color="#f5c2e7"/>
    </linearGradient>

    <!-- Accent gradient: pink to mauve -->
    <linearGradient id="g2" x1="0%" y1="100%" x2="100%" y2="0%">
      <stop offset="0%" stop-color="#f5c2e7"/>
      <stop offset="100%" stop-color="#cba6f7"/>
    </linearGradient>

    <!-- Icon gradient: vertical for the mark -->
    <linearGradient id="g3" x1="0%" y1="0%" x2="0%" y2="100%">
      <stop offset="0%" stop-color="#dbb9f9"/>
      <stop offset="50%" stop-color="#cba6f7"/>
      <stop offset="100%" stop-color="#f5c2e7"/>
    </linearGradient>

    <!-- Subtle glow for the logomark -->
    <filter id="softglow" x="-30%" y="-30%" width="160%" height="160%">
      <feGaussianBlur in="SourceAlpha" stdDeviation="6" result="blur"/>
      <feFlood flood-color="#cba6f7" flood-opacity="0.15" result="color"/>
      <feComposite in="color" in2="blur" operator="in" result="shadow"/>
      <feComposite in="SourceGraphic" in2="shadow" operator="over"/>
    </filter>

    <!-- Center dot glow -->
    <filter id="centerglow" x="-50%" y="-50%" width="200%" height="200%">
      <feGaussianBlur in="SourceGraphic" stdDeviation="3" result="blur"/>
      <feMerge>
        <feMergeNode in="blur"/>
        <feMergeNode in="SourceGraphic"/>
      </feMerge>
    </filter>
  </defs>

  <!-- ===================== LOGOMARK ===================== -->
  <g transform="translate(58, 40)" filter="url(#softglow)">

    <!-- Hexagonal frame — thin, elegant -->
    <path d="M 80 6 L 140 38 L 140 102 L 80 134 L 20 102 L 20 38 Z"
          fill="none" stroke="url(#g1)" stroke-width="1.5" opacity="0.4"/>

    <!-- Inner hex subtle fill -->
    <path d="M 80 14 L 134 42 L 134 98 L 80 126 L 26 98 L 26 42 Z"
          fill="#cba6f7" opacity="0.04"/>

    <!-- Capture frame — dashed square -->
    <rect x="48" y="38" width="64" height="64" rx="3"
          stroke="#cba6f7" stroke-width="2" stroke-dasharray="8 4"
          fill="none" opacity="0.75"/>

    <!-- Corner brackets — capture region indicators -->
    <!-- Top-left -->
    <path d="M 42,50 L 42,32 L 60,32" stroke="url(#g1)" stroke-width="2.5" stroke-linecap="round" fill="none" opacity="0.85"/>
    <!-- Top-right -->
    <path d="M 100,32 L 118,32 L 118,50" stroke="url(#g1)" stroke-width="2.5" stroke-linecap="round" fill="none" opacity="0.85"/>
    <!-- Bottom-left -->
    <path d="M 42,90 L 42,108 L 60,108" stroke="url(#g1)" stroke-width="2.5" stroke-linecap="round" fill="none" opacity="0.85"/>
    <!-- Bottom-right -->
    <path d="M 100,108 L 118,108 L 118,90" stroke="url(#g1)" stroke-width="2.5" stroke-linecap="round" fill="none" opacity="0.85"/>

    <!-- Crosshair lines — + shape -->
    <line x1="80" y1="44" x2="80" y2="62" stroke="#f5c2e7" stroke-width="1.8" stroke-linecap="round" opacity="0.7"/>
    <line x1="80" y1="78" x2="80" y2="96" stroke="#f5c2e7" stroke-width="1.8" stroke-linecap="round" opacity="0.7"/>
    <line x1="54" y1="70" x2="72" y2="70" stroke="#f5c2e7" stroke-width="1.8" stroke-linecap="round" opacity="0.7"/>
    <line x1="88" y1="70" x2="106" y2="70" stroke="#f5c2e7" stroke-width="1.8" stroke-linecap="round" opacity="0.7"/>

    <!-- Center dot with glow — bright node -->
    <circle cx="80" cy="70" r="5" fill="url(#g3)" opacity="0.9" filter="url(#centerglow)"/>
    <circle cx="80" cy="70" r="2.5" fill="#1e1e2e"/>
    <circle cx="80" cy="70" r="8" fill="none" stroke="#cba6f7" stroke-width="0.5" opacity="0.3"/>

    <!-- Corner bracket accent nodes -->
    <circle cx="42" cy="32" r="2" fill="#cba6f7" opacity="0.6"/>
    <circle cx="118" cy="32" r="2" fill="#dbb9f9" opacity="0.6"/>
    <circle cx="42" cy="108" r="2" fill="#dbb9f9" opacity="0.6"/>
    <circle cx="118" cy="108" r="2" fill="#f5c2e7" opacity="0.6"/>

    <!-- Subtle particles along crosshair lines -->
    <circle cx="80" cy="48" r="1.2" fill="#f5c2e7" opacity="0.4"/>
    <circle cx="80" cy="92" r="1.2" fill="#f5c2e7" opacity="0.4"/>
    <circle cx="58" cy="70" r="1.2" fill="#cba6f7" opacity="0.4"/>
    <circle cx="102" cy="70" r="1.2" fill="#cba6f7" opacity="0.4"/>

    <!-- Corner accents on hex -->
    <circle cx="80" cy="6" r="2" fill="#dbb9f9" opacity="0.3"/>
    <circle cx="80" cy="134" r="2" fill="#f5c2e7" opacity="0.3"/>
  </g>

  <!-- ===================== TYPOGRAPHY ===================== -->

  <!-- "HYDROSHOT" — clean, bold, wide-tracked -->
  <text x="430" y="108"
        font-family="'SF Pro Display', 'Inter', 'Segoe UI', 'Helvetica Neue', Arial, sans-serif"
        font-size="58" font-weight="800" letter-spacing="14"
        fill="url(#g1)" text-anchor="middle">HYDROSHOT</text>

  <!-- Thin separator line -->
  <line x1="270" y1="128" x2="590" y2="128"
        stroke="url(#g1)" stroke-width="0.8" opacity="0.25"/>

  <!-- Tagline — refined, subtle -->
  <text x="430" y="152"
        font-family="'SF Mono', 'JetBrains Mono', 'Fira Code', 'Cascadia Code', monospace"
        font-size="11.5" font-weight="400" letter-spacing="5.5"
        fill="#7f849c" text-anchor="middle">SCREENSHOT CAPTURE &amp; ANNOTATE</text>

  <!-- Version / descriptor — very subtle -->
  <text x="430" y="172"
        font-family="'SF Mono', 'JetBrains Mono', 'Fira Code', monospace"
        font-size="9" font-weight="400" letter-spacing="3"
        fill="#585b70" text-anchor="middle">RUST · WINIT · TINY-SKIA</text>
</svg>
